enum ProfileViewState{
  profile,
  security;
}

enum PopupMenuState {
  account,
  logout,
}

enum LayoutViewIndex {
  dashboardView,
  applicationsView,
  third,
  fourth;
  // profileView;
}

enum ApplicationFormState{
  cardPage,
  customerId,
  customerDetails,
  cardSelection,
  documents;
}